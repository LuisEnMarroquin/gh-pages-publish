{
  "name": "publish-gh-pages",
  "version": "1.0.0",
  "description": "Publish to GitHub Pages using your SSH key",
  "main": "index.js",
  "license": "MIT",
  "author": "LuisEnMarroquin <luis@marroquin.dev>",
  "publishConfig": { "registry": "https://npm.pkg.github.com/" },
  "scripts": {
    "start": "node clean.js && node index.js dev",
    "build": "ncc build index.js"
  },
  "dependencies": {
    "@actions/core": "^1.2.4",
    "@actions/github": "^4.0.0"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.22.3"
  },
  "standard": {
    "ignore": [
      "dist/*.js"
    ]
  }
}
